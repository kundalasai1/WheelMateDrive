import {NextResponse} from "next/server";import {getCurrentUser} from "@/lib/auth/session";import {connectDB} from "@/lib/db/mongoose";import {BookingModel} from "@/models/Booking";import {BookingEventModel} from "@/models/BookingEvent";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const user=await getCurrentUser();if(!user)return NextResponse.json({error:"Authentication required"},{status:401});const {id}=await params;await connectDB();const booking=await BookingModel.findById(id).lean();if(!booking)return NextResponse.json({error:"Not found"},{status:404});const allowed=user.role==="customer"?String(booking.customerId)===user.id:user.role==="driver"?String(booking.driverId)===user.id:true;if(!allowed)return NextResponse.json({error:"Forbidden"},{status:403});const events=await BookingEventModel.find({bookingId:id}).sort({createdAt:1}).lean();return NextResponse.json({booking:{...booking,_id:String(booking._id)},events});}
