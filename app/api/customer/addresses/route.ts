import {NextResponse} from "next/server";import {getCurrentUser} from "@/lib/auth/session";import {connectDB} from "@/lib/db/mongoose";import {CustomerAddressModel} from "@/models/CustomerAddress";
export async function POST(req:Request){const u=await getCurrentUser();if(!u||u.role!=="customer")return NextResponse.json({error:"Forbidden"},{status:403});const b=await req.json();if(!b.label||!b.address)return NextResponse.json({error:"Label and address are required"},{status:400});await connectDB();await CustomerAddressModel.create({customerId:u.id,label:String(b.label).trim(),address:String(b.address).trim(),lat:b.lat?Number(b.lat):undefined,lng:b.lng?Number(b.lng):undefined});return NextResponse.json({ok:true},{status:201})}
