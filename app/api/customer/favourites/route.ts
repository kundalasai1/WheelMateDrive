import {NextResponse} from "next/server";import {getCurrentUser} from "@/lib/auth/session";import {connectDB} from "@/lib/db/mongoose";import {FavouriteModel} from "@/models/Favourite";
export async function POST(req:Request){const u=await getCurrentUser();if(!u||u.role!=="customer")return NextResponse.json({error:"Forbidden"},{status:403});const b=await req.json();if(!b.name||!b.pickup||!b.destination)return NextResponse.json({error:"Name, pickup and destination are required"},{status:400});await connectDB();await FavouriteModel.create({...b,customerId:u.id});return NextResponse.json({ok:true},{status:201})}
