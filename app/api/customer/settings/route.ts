import {NextResponse} from "next/server";import {getCurrentUser} from "@/lib/auth/session";import {connectDB} from "@/lib/db/mongoose";import {CustomerProfileModel} from "@/models/CustomerProfile";
export async function PUT(req:Request){const u=await getCurrentUser();if(!u||u.role!=="customer")return NextResponse.json({error:"Forbidden"},{status:403});const b=await req.json();await connectDB();await CustomerProfileModel.updateOne({userId:u.id},{$set:{emailNotifications:!!b.emailNotifications,pushNotifications:!!b.pushNotifications,marketingMessages:!!b.marketingMessages,preferredLanguage:String(b.preferredLanguage??"English")}},{upsert:true});return NextResponse.json({ok:true})}
