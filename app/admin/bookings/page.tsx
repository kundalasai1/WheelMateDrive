import {requireUser} from "@/lib/auth/session";import {connectDB} from "@/lib/db/mongoose";import {BookingModel} from "@/models/Booking";import {DemoBookings} from "@/components/admin/demo-bookings";
export default async function Page(){const user=await requireUser(["admin","operations"]);if(user.isDemo)return <main className="container section"><p className="eyebrow">BOOKING OPERATIONS</p><h1>Manage every journey</h1><p className="muted admin-lead">Search bookings, update trip status and export operational data. Demo changes remain in this browser session.</p><DemoBookings/></main>;await connectDB();const rows=await BookingModel.find().sort({createdAt:-1}).limit(100).lean();return <main className="container section"><p className="eyebrow">BOOKING OPERATIONS</p><h1>Manage every journey</h1><div className="card table-wrap"><table><thead><tr><th>Booking</th><th>Route</th><th>Status</th><th>Fare</th><th>Schedule</th></tr></thead><tbody>{rows.map(b=><tr key={String(b._id)}><td><strong>{b.bookingNumber}</strong></td><td>{b.pickup.address} → {b.destination.address}</td><td><span className={`status-pill status-${b.status}`}>{b.status.replaceAll('_',' ')}</span></td><td>₹{Number(b.fare?.total||0).toLocaleString('en-IN')}</td><td>{new Date(b.scheduledAt).toLocaleString('en-IN')}</td></tr>)}</tbody></table></div></main>}
