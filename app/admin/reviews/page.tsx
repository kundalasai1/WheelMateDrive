import {requireUser} from "@/lib/auth/session";import {connectDB} from "@/lib/db/mongoose";import {RatingModel,type Rating} from "@/models/Rating";import {DemoReviews} from "@/components/admin/demo-reviews";
export default async function Page(){const user=await requireUser(["admin","support"]);if(user.isDemo)return <main className="container section"><p className="eyebrow">QUALITY CONTROL</p><h1>Ratings and review moderation</h1><p className="muted admin-lead">Publish genuine feedback, hide abusive content and flag reviews that require investigation.</p><DemoReviews/></main>;await connectDB();const rows=await RatingModel.find().sort({createdAt:-1}).limit(100).lean();const avg=rows.length?rows.reduce((s:number,r:Rating)=>s+r.score,0)/rows.length:0;return <main className="container section"><p className="eyebrow">QUALITY CONTROL</p><h1>Ratings and review moderation</h1><div className="metric-grid"><article className="metric"><span>Average quality score</span><strong>{avg.toFixed(1)}/5</strong></article><article className="metric"><span>Reviews</span><strong>{rows.length}</strong></article></div><div className="card table-wrap"><table><thead><tr><th>Date</th><th>Direction</th><th>Score</th><th>Feedback</th><th>Status</th></tr></thead><tbody>{rows.map(r=><tr key={String(r._id)}><td>{new Date(r.createdAt).toLocaleDateString('en-IN')}</td><td>{r.role}</td><td>{'★'.repeat(r.score)}</td><td>{r.comment||'—'}</td><td>{r.status}</td></tr>)}</tbody></table></div></main>}
