import {requireUser} from "@/lib/auth/session";import {connectDB} from "@/lib/db/mongoose";import {DriverLedgerModel} from "@/models/DriverLedger";import {WithdrawalRequestModel} from "@/models/WithdrawalRequest";import {WithdrawalForm} from "@/components/driver/withdrawal-form";
export default async function Page(){const u=await requireUser(["driver"]);await connectDB();const rows=await DriverLedgerModel.find({driverId:u.id}).sort({createdAt:-1}).lean();const withdrawals=await WithdrawalRequestModel.find({driverId:u.id}).sort({createdAt:-1}).lean();const now=new Date(),day=new Date(now.getFullYear(),now.getMonth(),now.getDate()),week=new Date(now.getTime()-7*86400000),month=new Date(now.getFullYear(),now.getMonth(),1);const sum=(d:Date)=>rows.filter((r:any)=>new Date(r.createdAt)>=d).reduce((s:any,r:any)=>s+Number(r.amount),0);const balance=rows.reduce((s:any,r:any)=>s+Number(r.amount),0);return <main className="container section"><p className="eyebrow">DRIVER FINANCE</p><h1>Earnings & payouts</h1><div className="metric-grid"><article className="metric"><span>Today</span><strong>₹{sum(day).toLocaleString('en-IN')}</strong></article><article className="metric"><span>7 days</span><strong>₹{sum(week).toLocaleString('en-IN')}</strong></article><article className="metric"><span>This month</span><strong>₹{sum(month).toLocaleString('en-IN')}</strong></article><article className="metric"><span>Available balance</span><strong>₹{balance.toLocaleString('en-IN')}</strong></article></div><WithdrawalForm balance={balance}/><div className="card table-wrap"><table><thead><tr><th>Date</th><th>Type</th><th>Description</th><th>Status</th><th>Amount</th></tr></thead><tbody>{rows.map((r:any)=><tr key={String(r._id)}><td>{new Date(r.createdAt).toLocaleDateString('en-IN')}</td><td>{r.kind}</td><td>{r.description}</td><td>{r.status}</td><td>₹{Number(r.amount).toLocaleString('en-IN')}</td></tr>)}</tbody></table></div><h2>Payout history</h2><div className="card table-wrap"><table><tbody>{withdrawals.map((w:any)=><tr key={String(w._id)}><td>{new Date(w.createdAt).toLocaleDateString('en-IN')}</td><td>₹{w.amount.toLocaleString('en-IN')}</td><td>{w.status}</td></tr>)}{!withdrawals.length&&<tr><td>No withdrawal requests.</td></tr>}</tbody></table></div></main>}
