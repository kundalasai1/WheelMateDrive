import {notFound} from "next/navigation";import {requireUser} from "@/lib/auth/session";import {connectDB} from "@/lib/db/mongoose";import {BookingModel} from "@/models/Booking";import {BookingEventModel} from "@/models/BookingEvent";import {BookingTimeline} from "@/components/customer/booking-timeline";
export default async function Page({params}:{params:Promise<{id:string}>}){const u=await requireUser(["customer"]);const {id}=await params;await connectDB();const b=await BookingModel.findOne({_id:id,customerId:u.id}).lean();if(!b)notFound();const events=await BookingEventModel.find({bookingId:id}).sort({createdAt:1}).lean();return <main><div className="page-head"><div><p className="eyebrow">BOOKING DETAILS</p><h1>{b.bookingNumber}</h1></div><span className="badge">{b.status.replaceAll("_"," ")}</span></div><div className="two-col"><div className="stack"><article className="card metric"><h2>Journey</h2><p><strong>Pickup:</strong> {b.pickup.address}</p><p><strong>Destination:</strong> {b.destination.address}</p><p><strong>Scheduled:</strong> {new Date(b.scheduledAt).toLocaleString("en-IN")}</p><p><strong>Vehicle:</strong> {b.vehicleType} · {b.transmissionType}</p></article><article className="card metric"><h2>Trip progress</h2><BookingTimeline status={b.status}/></article><article className="card metric"><h2>Activity log</h2>{events.map(e=><div key={String(e._id)} style={{padding:"10px 0",borderBottom:"1px solid var(--border)"}}><strong>{e.type.replaceAll("_"," ")}</strong><p className="muted">{new Date(e.createdAt).toLocaleString("en-IN")}</p></div>)}</article></div><aside className="stack"><article className="card metric"><h2>Fare</h2><p>Base fare: ₹{b.fare.baseFare}</p><p>Distance: ₹{b.fare.distanceFare}</p><p>Duration: ₹{b.fare.durationFare}</p><p>Surcharges: ₹{b.fare.surcharges}</p><p>Platform fee: ₹{b.fare.platformFee}</p><p>Tax: ₹{b.fare.tax}</p><strong>Total: ₹{b.fare.total}</strong></article><article className="card metric"><h2>Safety</h2><p className="muted">Emergency contact: {b.emergencyContact||"Not provided"}</p><p>Payment: {b.paymentMethod.toUpperCase()} · {b.paymentStatus}</p></article></aside></div></main>}
