"use client";import {useState} from "react";
export function WithdrawalForm({balance}:{balance:number}){const [amount,setAmount]=useState(""),[message,setMessage]=useState("");async function submit(){const r=await fetch('/api/earnings/withdrawals',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({amount:Number(amount)})});const b=await r.json();setMessage(r.ok?'Withdrawal request submitted.':b.error||'Request failed.');if(r.ok)setAmount('')}return <div className="card inline-form"><h2>Withdrawal request</h2><input value={amount} onChange={e=>setAmount(e.target.value)} type="number" min="1" max={Math.max(balance,1)} placeholder="Amount"/><button type="button" className="btn btn-primary" onClick={submit}>Request payout</button>{message&&<span>{message}</span>}</div>}
