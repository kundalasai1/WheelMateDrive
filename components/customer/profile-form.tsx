"use client";import {useState} from "react";import {useRouter} from "next/navigation";export function ProfileForm({profile}:{profile:{fullName?:string;address?:string;emergencyContact?:string}}){const [msg,setMsg]=useState("");const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const payload=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/customer/profile",{method:"PUT",headers:{"content-type":"application/json"},body:JSON.stringify(payload)});setMsg(r.ok?"Profile updated":"Unable to update");router.refresh()}return <form className="card auth-form" onSubmit={submit}><label>Full name<input name="fullName" defaultValue={profile.fullName} required/></label><label>Address<textarea name="address" defaultValue={profile.address}/></label><label>Emergency contact<input name="emergencyContact" defaultValue={profile.emergencyContact}/></label>{msg&&<p className={msg.includes("updated")?"form-success":"form-error"}>{msg}</p>}<button className="btn btn-primary">Update profile</button></form>}
