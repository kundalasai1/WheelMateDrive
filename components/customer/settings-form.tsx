"use client";import {useState} from "react";export function SettingsForm({settings}:{settings:{emailNotifications?:boolean;pushNotifications?:boolean;marketingMessages?:boolean;preferredLanguage?:string}}){const [saved,setSaved]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);await fetch("/api/customer/settings",{method:"PUT",headers:{"content-type":"application/json"},body:JSON.stringify({emailNotifications:f.has("emailNotifications"),pushNotifications:f.has("pushNotifications"),marketingMessages:f.has("marketingMessages"),preferredLanguage:f.get("preferredLanguage")})});setSaved(true)}return <form className="card auth-form" onSubmit={submit}><label><input type="checkbox" name="emailNotifications" defaultChecked={settings.emailNotifications}/> Email notifications</label><label><input type="checkbox" name="pushNotifications" defaultChecked={settings.pushNotifications}/> Push notifications</label><label><input type="checkbox" name="marketingMessages" defaultChecked={settings.marketingMessages}/> Offers and marketing</label><label>Preferred language<select name="preferredLanguage" defaultValue={settings.preferredLanguage??"English"}><option>English</option><option>Telugu</option><option>Hindi</option><option>Kannada</option><option>Tamil</option></select></label>{saved&&<p className="form-success">Settings saved</p>}<button className="btn btn-primary">Save settings</button></form>}
