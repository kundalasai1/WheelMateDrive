export type PricingInput={baseFare:number;baseKilometres:number;perKilometreFare:number;perHourFare:number;minimumFare:number;nightSurcharge:number;weekendSurcharge:number;outstationAllowance:number;driverFoodAllowance:number;overnightAllowance:number;platformFee:number;taxPercent:number;surgeMultiplier:number;distanceKm:number;durationHours:number;scheduledAt:Date;tripType:string};
export function calculateFare(i:PricingInput){const distanceFare=Math.max(0,i.distanceKm-i.baseKilometres)*i.perKilometreFare;const durationFare=i.durationHours*i.perHourFare;const hour=i.scheduledAt.getHours();const night=hour>=22||hour<6?i.nightSurcharge:0;const weekend=[0,6].includes(i.scheduledAt.getDay())?i.weekendSurcharge:0;const outstation=["outstation","round_trip","one_way"].includes(i.tripType)?i.outstationAllowance+i.driverFoodAllowance+(i.durationHours>=24?i.overnightAllowance:0):0;const subtotal=(i.baseFare+distanceFare+durationFare+night+weekend+outstation)*i.surgeMultiplier;const beforeTax=Math.max(i.minimumFare,subtotal)+i.platformFee;const tax=beforeTax*i.taxPercent/100;return {baseFare:round(i.baseFare),distanceFare:round(distanceFare),durationFare:round(durationFare),surcharges:round(night+weekend+outstation+(subtotal-(i.baseFare+distanceFare+durationFare+night+weekend+outstation))),platformFee:round(i.platformFee),tax:round(tax),discount:0,total:round(beforeTax+tax),currency:"INR" as const};}
const round=(n:number)=>Math.round((n+Number.EPSILON)*100)/100;
