import test from "node:test";import assert from "node:assert/strict";
function fare(i){const distanceFare=Math.max(0,i.distanceKm-i.baseKilometres)*i.perKilometreFare;const durationFare=i.durationHours*i.perHourFare;const hour=i.scheduledAt.getHours();const night=hour>=22||hour<6?i.nightSurcharge:0;const weekend=[0,6].includes(i.scheduledAt.getDay())?i.weekendSurcharge:0;const out=["outstation","round_trip","one_way"].includes(i.tripType)?i.outstationAllowance+i.driverFoodAllowance+(i.durationHours>=24?i.overnightAllowance:0):0;const subtotal=(i.baseFare+distanceFare+durationFare+night+weekend+out)*i.surgeMultiplier;const before=Math.max(i.minimumFare,subtotal)+i.platformFee;return Math.round((before+before*i.taxPercent/100)*100)/100}
test("calculates local fare",()=>assert.equal(fare({baseFare:200,baseKilometres:5,perKilometreFare:10,perHourFare:50,minimumFare:250,nightSurcharge:0,weekendSurcharge:0,outstationAllowance:0,driverFoodAllowance:0,overnightAllowance:0,platformFee:20,taxPercent:5,surgeMultiplier:1,distanceKm:10,durationHours:2,scheduledAt:new Date("2026-07-27T10:00:00"),tripType:"local"}),388.5));
test("adds outstation allowances",()=>assert.ok(fare({baseFare:500,baseKilometres:10,perKilometreFare:12,perHourFare:0,minimumFare:500,nightSurcharge:0,weekendSurcharge:0,outstationAllowance:300,driverFoodAllowance:200,overnightAllowance:500,platformFee:0,taxPercent:0,surgeMultiplier:1,distanceKm:100,durationHours:25,scheduledAt:new Date("2026-07-27T10:00:00"),tripType:"outstation"})>2000));
