import {Schema,model,models} from "mongoose";
const schema=new Schema({bookingId:{type:Schema.Types.ObjectId,ref:"Booking",required:true,index:true},fromUserId:{type:Schema.Types.ObjectId,ref:"User",required:true},toUserId:{type:Schema.Types.ObjectId,ref:"User",required:true,index:true},role:{type:String,enum:["customer_to_driver","driver_to_customer"],required:true},score:{type:Number,min:1,max:5,required:true},comment:{type:String,maxlength:1000},tags:[String],status:{type:String,enum:["published","pending","hidden"],default:"published",index:true}},{timestamps:true});schema.index({bookingId:1,fromUserId:1},{unique:true});export const RatingModel=models.Rating??model("Rating",schema);
