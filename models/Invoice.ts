import type {Model as MongooseModel, InferSchemaType as MongooseInferSchemaType} from "mongoose";
import {Schema,model,models} from "mongoose";
const schema=new Schema({invoiceNumber:{type:String,required:true,unique:true,index:true},bookingId:{type:Schema.Types.ObjectId,ref:"Booking",required:true,index:true},customerId:{type:Schema.Types.ObjectId,ref:"User",required:true,index:true},company:{name:String,gstin:String,address:String,email:String,phone:String},subtotal:Number,cgst:Number,sgst:Number,igst:Number,total:{type:Number,required:true},currency:{type:String,default:"INR"},issuedAt:{type:Date,default:Date.now},emailedAt:Date},{timestamps:true});export const InvoiceModel=(models.Invoice??model("Invoice",schema)) as MongooseModel<MongooseInferSchemaType<typeof schema>>;
