import { NextRequest } from "next/server";
export function getClientIp(req:NextRequest){return req.headers.get("x-forwarded-for")?.split(",")[0]?.trim() ?? req.headers.get("x-real-ip") ?? "unknown"}
export function assertSameOrigin(req:NextRequest){const origin=req.headers.get("origin");if(!origin)return;const supplied=new URL(origin).origin;const requestOrigin=new URL(req.url).origin;const configured=[process.env.NEXT_PUBLIC_APP_URL,process.env.APP_URL,process.env.NEXTAUTH_URL].filter((v):v is string=>Boolean(v)).map(v=>new URL(v).origin);const development=process.env.NODE_ENV==="development"?["http://localhost:3000","http://127.0.0.1:3000","http://localhost:3001","http://127.0.0.1:3001"]:[];const allowed=new Set([requestOrigin,...configured,...development]);if(!allowed.has(supplied))throw new Error("INVALID_ORIGIN")}
